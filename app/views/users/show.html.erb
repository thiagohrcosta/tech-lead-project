<div class="container">
  <div class="row">
    <div class="col-lg-6">
      <img src="<%= @user.photo %>">

    </div>
    <div class="col-lg-6">
      <%= @user.first_name %>
      <%= @user.email %>
    </div>
  </div>
</div>

<% @user.contracts.each do |contract| %>
  <p>
  <%= contract.developer_profile_id %>
  </p>
<% end %>

<h2>
</h2>
